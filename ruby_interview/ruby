1: block.

def geet
   yeild 
end

geet {puts "shankar"}

2: Proc.

p = Proc.new { |a, b| puts "#{a} #{b}" }

p.call(10)

3: Lambda.

l = lambda { |a, b| puts "#{a} #{b}" }

l.call(10, 20)

square = ->(x) { x * x }

puts square.call(5)

4: Module:

module Test
  def show
    puts "Module"
  end
end
include and instance metho bna deta h object bnake call hota hai.
class User
  include Test
end


class Admin
  extend Test
end

extend instance method ko class method bna deta hai

class Manager
  prepend Test

  def show
    puts "Class"
  end
end

Module ko first priority deta hai include jesa hi hai.

6: Mixin.

Mixin is a way to share reusable behavior among multiple classes using modules without inheritance.

module Flyable
  def fly
    puts "I can fly"
  end
end

class Bird
  include Flyable
end

class Plane
  include Flyable
end



7: Exception Handling.

begin
  10/0
rescue ZeroDivisionError
  puts "Error"
end

8: Monkey Patching kya hai?

Monkey Patching ka matlab hai kisi existing class ya method ko runtime par modify karna ya override karna, bina original source code ko change kiye.

class String
  def greeting
    "Hello #{self}"
  end
end

puts "Shankar".greeting
# Hello Shankar


class String
  def upcase
    "Monkey Patched!"
  end
end

puts "hello".upcase

9: setter and getter.
class User
  def name
    @name
  end

  def name=(value)
    @name = value
  end
end

user = User.new
user.name = "Shankar"
puts user.name

class User
  attr_reader :name

  def initialize
    @name = "Shankar"
  end
end

user = User.new
puts user.name


Setter only

class User
  attr_writer :name
end

user = User.new
user.name = "Shankar"


both setter and getter

class User
  attr_accessor :name
end

user = User.new
user.name = "Shankar"


10: String and Symbol. 

name = "Shankar"
Mutable (change ho sakti hai)
Har baar naya object banta hai

puts "ruby".object_id
puts "ruby".object_id

Different object IDs mil sakte hain.

Symbol

name = :shankar

Immutable (change nahi ho sakta)
Same symbol ka ek hi object memory me rehta hai

puts :ruby.object_id
puts :ruby.object_id

Dono same object_id denge.

user = {
  name: "Shankar",
  age: 25
}

11: Load and Require.


1. require

File ko sirf ek baar load karta hai.

require './test'User.left_joins(:orders)
    .where(orders: { id: nil })
require './test'

File ko har baar reload karta hai.

load './test.rb'
load './test.rb'

12: Closure kya hota hai Ruby me?

Closure ek aisa block/proc/lambda hota hai jo apne surrounding scope ke variables ko yaad rakhta hai, even jab original method execute ho chuki ho.


x = 10

p = Proc.new { puts x }

x = 20

p.call










User.left_joins(:orders)
    .where(orders: { id: nil })























how to create job in sidekiq

User.left_joins(:orders)
    .where(orders: { id: nil })
    
    
ordered_user_ids = Order.pluck(:user_id)

User.where.not(id: ordered_user_ids)    

puts user.name
Interview me important topicsUser.left_joins(:orders)
    .where(orders: { id: nil })
Duck typing
send
public_send
define_method
method_missing
respond_to?
class_eval
instance_eval
singleton methods
included, extended
Dynamic method creation
select, reject, and grep
numbers.map { |num| num * 2 }
How do you remove nil values in array using ruby?
# Names all uppercase
names.collect { |name| name.upcase }

# Split numbers based on even or odd
numbers.partition { |num| num.even? }
Agar Ruby/Rails interview ki preparation kar rahe ho, to send, define_method, method_missing, class_eval, include/extend/prepend sabse zyada puchhe jate hain.


What is the difference between the Object methods clone and dup?


Datadog

Datadog sirf Rails nahi, pura infrastructure monitor karta hai.

Monitor:

Rails App
Puma
Nginx
Redis
Postgres
Docker
AWS







1. New Relic

New Relic ek APM (Application Performance Monitoring) tool hai.

Ye batata hai:

Kaunsa API slow hai
Kaunsi SQL query slow hai
Memory usage kitni hai
Error kaha aa rahi hai
Throughput (requests/minute)

gem 'newrelic_rpm'



SELECT *
FROM customer
LEFT JOIN orders
ON customer.customer_id = orders.customer_id;





. Bullet

Rails interviews me bahut important.

Bullet detect karta hai:

N+1 Queries

Example:

@users = User.all

@users.each do |user|
  puts user.posts.count
end

gem 'bullet'


callback

Callbacks are methods that get executed automatically at specific stages of an ActiveRecord object's lifecycle, such as before validation, before save, after create, or before destroy. They are used to perform tasks like data normalization, token generation, sending emails, and maintaining related records.



What are the disadvantages of polymorphic associations?

* Why is a foreign key difficult to implement with a polymorphic association?

* How can authentication be implemented between a React frontend and a Rails backend?

* How does JWT authentication flow work with React and Rails?

* Which credentials are used in the JWT authentication flow?

* If the user sends something else in the authentication header, what happens?

* How do we implement logout when using JWT authentication?

* Where is the access token removed during logout?

ans - In a typical stateless JWT implementation, the access token is removed from the client side during logout—for example, from localStorage, sessionStorage, or application memory. Rails does not normally remove the token because it doesn't maintain a server-side session for that JWT. If the JWT is stored in an HttpOnly cookie, the Rails backend clears or expires the cookie. For immediate token invalidation, we can additionally implement token revocation or a denylist.

* If we remove the access token from React but the JWT is still valid on Rails, how do we actually log out the user?


"Removing the JWT from React logs the user out from that client, but it does not invalidate the JWT on the server because JWTs are stateless. If we need immediate server-side logout, we implement token revocation or a denylist. During logout, Rails marks the token's unique identifier as revoked, and every subsequent request checks whether the token has been revoked. If it has, Rails returns 401 Unauthorized."


* What is database connection pooling?

ans - Database connection pooling is a technique where an application maintains a pool of reusable database connections instead of creating a new database connection for every request.

production:
  adapter: postgresql
  database: myapp
  pool: 5
  
pool: 5 means Rails can maintain up to 5 database connections for that pool.  

* If we have 20 connections in the connection pool, what does that mean?
ans - If the connection pool is 20, it means a Rails process can maintain up to 20 database connections in its pool. The pool size is generally decided based on the number of application threads, number of processes, and the database's maximum connection limit. We need to make sure the total connections from all Rails processes and other services don't exceed the database capacity.

PostgreSQL max_connections = 100

Puma processes = 4
Threads per process = 5
Pool per process = 5



* How do we decide the number of database connections?
ans - We decide the database connection pool size mainly based on application concurrency and the database's capacity.

1. Start with Rails/Puma threads

Suppose Puma is configured:

threads 5, 5

That means one Rails process can handle up to 5 threads.

A common starting point is:

pool: 5

So:

1 Rails process
× 5 threads
= 5 DB connections

We decide the database connection pool size mainly based on application concurrency and the database's capacity.

1. Start with Rails/Puma threads

Suppose Puma is configured:

threads 5, 5

That means one Rails process can handle up to 5 threads.

A common starting point is:

pool: 5

So:

1 Rails process
× 5 threads
= 5 DB connections

* Why don't we use 5,000 database connections?
ans - Because database connections are expensive resources, and a database cannot efficiently handle thousands of active connections.

For example, if we configure:

pool: 5000

that does not mean our application becomes 1000× faster.

Why not 5,000?

1. Each connection consumes resources

A PostgreSQL connection uses memory and CPU on the database server.

5000 connections
      ↓
High memory usage
      ↓
More CPU/context switching
      ↓
Database becomes slower

2. PostgreSQL has a connection limit

For example:

max_connections = 200

If Rails tries to use 5,000 connections, the database cannot accept them.

3. Most connections would be idle

Suppose your application has only 20 concurrent database operations:

5000 connections
├── 20 being used
└── 4980 mostly idle

You're consuming resources without getting useful performance.

4. More connections ≠ more concurrency

The database itself has limited CPU, memory, disk I/O, locks, etc.

If 5,000 queries hit the database simultaneously, they can actually compete for resources and make everything slower.

Better approach

Instead of creating thousands of database connections, we use a reasonable connection pool:

Rails



* How do we handle 1 lakh database connections?
ans - If we literally have 100,000 database connections, we don't allow all of them to connect directly to PostgreSQL. We use connection pooling and a pooler such as PgBouncer to multiplex a large number of client connections over a much smaller number of actual database connections. If database capacity is still insufficient, we scale using read replica	s, caching, and database sharding or other distributed database architecture.

* What is a load balancer, and how does increasing the number of load balancers help?
“A load balancer distributes incoming requests across multiple application servers. It improves availability and scalability by preventing a single server from handling all the traffic. When traffic increases, we usually add more application instances behind the load balancer. We don't simply add more load balancers; instead, the load-balancing layer itself is made highly available, while the application layer is horizontally scaled.”
* If the problem is specifically handling 1 lakh database connections, how should we solve it?

* What is database replication?

* If a Sidekiq job fails, what happens to it?

* If Sidekiq is down and then comes back up, what happens to the job?

* What happens if the job was already inside a Sidekiq worker and not in Redis?

* How do we safely remove a column from a database table containing 5 million records?

* How do we make sure the column is no longer being used before removing it?

* What is GIL in Ruby?

* When can another Ruby thread execute if one thread is holding the GIL?

* Does a database query lock the GIL?

* What are the disadvantages of GIL?

* Design an API that needs to return 1 million records. How would you handle it?

* What is cursor pagination?

* What ID is used behind the scenes in cursor pagination?

* Given n and k, how do you find the kth permutation without using predefined permutation methods?

* Provide the full Ruby code with input for the kth permutation problem.

* Provide the code using the given example inputs.

* Provide the kth permutation code without using downto.

* Explain the kth permutation code.

* What is the idea behind the kth permutation algorithm?

* What is k in the kth permutation problem?

* Explain the kth permutation code as an interview answer.

* Explain the whole kth permutation concept.

* Explain the while loop logic in the kth permutation code.

* Explain the while loop in a paragraph that can be read directly in an interview.

* Why do we use remaining - 1 in the kth permutation algorithm?

* What is the time and space complexity of the kth permutation code?


